$(".navbar a").click(function(){
  $("body,html").animate({
   scrollTop:$("#" + $(this).data('value')).offset().top
  },1000)
  
 })
jQuery(document).ready(function($) {
 	let text = []
  if ($(window).width() < 1185) {
 	$('img').foreach().hasClass('offset-5').removeClass
  } 

 
});